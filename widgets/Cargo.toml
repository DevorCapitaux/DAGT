[package]
name = "dagt-widgets"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
dagt-core = { path = "../core" }
dagt-platform = { path = "../platform" }
dagt-gl = { path = "../gl" }
dagt-primitives = { path = "../primitives" }
dagt-fonts = { path = "../fonts" }
